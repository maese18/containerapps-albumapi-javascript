name: Trigger auto deployment for album-api-4

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ buildpack ]
    paths:
    - 'src/**'
    - '.github/workflows/album-api-4-AutoDeployTrigger-2787729e-f6ef-44a8-8457-115b307f999b.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.ALBUMAPI4_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/src 
          registryUrl: ca11e6cfa307acr.azurecr.io
          registryUsername: ${{ secrets.ALBUMAPI4_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ALBUMAPI4_REGISTRY_PASSWORD }}
          containerAppName: album-api-4
          resourceGroup: album-containerapps_3
          imageToBuild: ca11e6cfa307acr.azurecr.io/album-api-4:${{ github.sha }}
          
            

     

